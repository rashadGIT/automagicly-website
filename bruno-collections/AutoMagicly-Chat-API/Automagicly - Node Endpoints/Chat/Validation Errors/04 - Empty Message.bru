meta {
  name: 04 - Empty Message
  type: http
  seq: 4
}

post {
  url: {{auditBaseUrl}}/api/chat
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Origin: {{auditBaseUrl}}
}

body:json {
  {
    "message": "",
    "sessionId": "empty-msg-test-001"
  }
}

docs {
  Tests that empty messages are rejected.
  Expected: 400 status with validation error
}

tests {
  test("Status is 400", function() {
    expect(res.status).to.equal(400);
  });

  test("Response has validation error", function() {
    expect(res.body).to.have.property('reply');
    expect(res.body.reply).to.include('Invalid request data');
  });
}
