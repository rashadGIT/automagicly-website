meta {
  name: 02 - Invalid Content Type
  type: http
  seq: 2
}

post {
  url: {{auditBaseUrl}}/api/chat
  body: text
  auth: none
}

headers {
  Content-Type: text/plain
  Origin: {{auditBaseUrl}}
}

body:text {
  Hello from a plain text body
}

tests {
  test("Status is 400", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Response has invalid content type reason", function() {
    expect(res.body).to.have.property('reason');
    expect(res.body.reason).to.equal('invalid_content_type');
  });
}
