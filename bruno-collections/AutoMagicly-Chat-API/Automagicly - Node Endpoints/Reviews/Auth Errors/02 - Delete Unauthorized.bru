meta {
  name: 02 - Delete Unauthorized
  type: http
  seq: 2
}

delete {
  url: {{auditBaseUrl}}/api/reviews?id=review-1234567890-1
  auth: none
}

headers {
  Origin: {{auditBaseUrl}}
}

tests {
  test("Status is 403", function() {
    expect(res.status).to.equal(403);
  });
  
  test("Response has unauthorized error", function() {
    expect(res.body).to.have.property('error');
    expect(res.body.error).to.include('admin access');
  });
}
