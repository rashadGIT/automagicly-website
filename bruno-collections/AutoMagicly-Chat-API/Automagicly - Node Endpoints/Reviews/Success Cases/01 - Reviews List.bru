meta {
  name: 01 - Reviews List
  type: http
  seq: 1
}

get {
  url: {{auditBaseUrl}}/api/reviews?status=approved
  auth: none
}

headers {
  Origin: {{auditBaseUrl}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("Response has reviews array", function() {
    expect(res.body).to.have.property('reviews');
  });
}
