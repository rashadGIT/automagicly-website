meta {
  name: 08 - Conversation Memory (Step 1)
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/webhook/chat
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  X-API-Key: {{apiKey}}
}

body:json {
  {
    "message": "My name is John",
    "sessionId": "test-memory-001",
    "userEmail": "john@test.com"
  }
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("Response acknowledges the name", function() {
    const reply = res.body.reply.toLowerCase();
    expect(reply).to.include('john');
  });
}

docs {
  # Conversation Memory Test - Step 1
  
  This is the first message in a conversation where the user introduces themselves.
  
  Run this first, then run "08 - Conversation Memory (Step 2)" to test if the AI remembers.
}
