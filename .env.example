# ============================================================================
# ENVIRONMENT FILES SETUP
# ============================================================================
# This project uses multiple environment files:
# - .env.local       → Local development (test webhooks)
# - .env.production  → Production (production webhooks) - NOT committed to git
# - .env.example     → Template (this file)
#
# Copy this file to .env.local for development
# Copy this file to .env.production and update with production webhooks
# ============================================================================

# n8n Webhook URLs (Public - Frontend)
# These are called from the browser, so they must be NEXT_PUBLIC_

# Custom Booking System (replaces Calendly)
# See N8N_BOOKING_SETUP.md for complete n8n workflow setup
# DEVELOPMENT: Use /webhook-test/ URLs
# PRODUCTION:  Use /webhook/ URLs
NEXT_PUBLIC_N8N_AUDIT_WEBHOOK_URL=https://your-n8n-instance.com/webhook-test/booking

# Other Form Webhooks
NEXT_PUBLIC_N8N_REVIEWS_WEBHOOK_URL=https://your-n8n-instance.com/webhook-test/reviews
NEXT_PUBLIC_N8N_REVIEW_GENERATOR_WEBHOOK_URL=https://your-n8n-instance.com/webhook-test/review-generator
NEXT_PUBLIC_N8N_REFERRALS_WEBHOOK_URL=https://your-n8n-instance.com/webhook-test/referrals
NEXT_PUBLIC_N8N_WAITLIST_WEBHOOK_URL=https://your-n8n-instance.com/webhook-test/waitlist

# Calendly URL (Optional - replaced by custom booking system)
# NEXT_PUBLIC_CALENDLY_URL=https://calendly.com/your-handle/ai-audit

# n8n RAG Chatbot Webhook (Server-only - Backend)
# This is called from the API route, so it should NOT be NEXT_PUBLIC_
# See N8N_CHATBOT_SETUP.md for complete RAG chatbot setup
# The chatbot only answers from your custom knowledge base documents
N8N_CHAT_WEBHOOK_URL=https://your-n8n-instance.com/webhook/chat

# Google Calendar API (Server-only - Backend)
# Used to automatically block busy dates in the booking system
# See GOOGLE_CALENDAR_SETUP.md for instructions on getting these credentials
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"
GOOGLE_CALENDAR_ID=your-calendar-id@group.calendar.google.com

# Supabase Configuration
# Get these from: https://supabase.com/dashboard → Your Project → Settings → API
# See SUPABASE_SETUP_GUIDE.md for setup instructions
NEXT_PUBLIC_SUPABASE_URL=https://niuadokiehxkethhrhtp.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_XmaUq6U7eudZXD4b6tno-A_us4_11e-
SUPABASE_SERVICE_ROLE_KEY=sb_secret__GHz_nGyR8WLA4NuuqmYQg_zW55i4yA

# Admin Dashboard Password
# Used for /admin/reviews page
# CHANGE THIS TO A SECURE PASSWORD!
NEXT_PUBLIC_ADMIN_PASSWORD=your-secure-password-here

# DynamoDB Configuration (Server-only - Backend)
# Get these from AWS IAM → Users → automagicly-user → Security credentials
DB_ACCESS_KEY_ID=your-access-key-id
DB_SECRET_ACCESS_KEY=your-secret-access-key
REGION=us-east-1
